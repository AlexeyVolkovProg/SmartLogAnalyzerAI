apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: smart-log-analyzer
  labels:
    app: app
spec:
  selector:
    app: app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort